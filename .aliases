alias dirs="dirs -v"
alias tmux='tmux -2'
alias ls='ls -lh'
alias la='ls -lah'
alias :q='exit'
alias sagi="sudo apt-get install"
alias cl="clear"

if type nvim >/dev/null 2>/dev/null; then
  alias vim=nvim
fi
