name: monitor
root: ~/

# on_project_first_start: ~/Viriciti/scripts/start-up.sh

# on_project_stop: sudo systemctl stop openvpn@virivpn && docker ps -aq | xargs docker stop

windows:
  - portal:
      # layout: main-horizontal
      panes:
        - monitor:
          - htop
